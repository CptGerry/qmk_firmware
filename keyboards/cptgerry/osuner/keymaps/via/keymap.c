#include QMK_KEYBOARD_H

const uint16_t PROGMEM keymaps[][MATRIX_ROWS][MATRIX_COLS] = {
      [0] = LAYOUT_2(
      KC_Z, KC_X, 
      KC_LEFT, KC_ENT, KC_RGHT
      ),
      [1] = LAYOUT_2(
      KC_Z, KC_X, 
      KC_LEFT, KC_ENT, KC_RGHT
      ),
      [2] = LAYOUT_2(
      KC_Z, KC_X, 
      KC_LEFT, KC_ENT, KC_RGHT
      ),
      [3] = LAYOUT_2(
      KC_Z, KC_X, 
      KC_LEFT, KC_ENT, KC_RGHT
      )
};
