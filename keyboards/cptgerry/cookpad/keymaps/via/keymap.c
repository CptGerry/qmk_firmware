#include "cookpad_pad.h"

const uint16_t PROGMEM keymaps[][MATRIX_ROWS][MATRIX_COLS] = {
      [0] = LAYOUT(
      KC_Z, KC_X, KC_C, 
      KC_LEFT, KC_ENT, KC_RGHT
      ),
      [1] = LAYOUT(
      KC_Z, KC_X, KC_C, 
      KC_LEFT, KC_ENT, KC_RGHT
      ),
      [2] = LAYOUT(
      KC_Z, KC_X, KC_C,
      KC_LEFT, KC_ENT, KC_RGHT
      ),
      [3] = LAYOUT(
      KC_Z, KC_X, KC_C,
      KC_LEFT, KC_ENT, KC_RGHT
      )
};
